{
  "name": "cgdl-lib",
  "version": "0.1.0",
  "type": "module",
  "private": true,
  "description": "CGDL graph-building linrary (ESM-only).",
  "license": "MIT",
  "scripts": {
    "test": "vitest run",
    "build": "tsc -p tsconfig.json",
    "clean": "rmdir /s /q dist 2>nul || exit /b 0",
    "prepublishOnly": "npm run clean && npm test && npm run build"
  },
  "devDependencies": {
    "typescript": "^5.8.0",
    "vitest": "^2.1.0"
  },
  "overrides": {
    "esbuild": "^0.25.0"
  },
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "default": "./dist/index.js"
    }
  },
  "files": [
    "dist",
    "README.md",
    "LICENSE"
  ]

}

